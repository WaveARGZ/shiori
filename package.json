{
  "name": "shiori",
  "version": "0.1.1",
  "description": "栞 — a browser that manages what you were in the middle of, not where you were.",
  "author": {
    "name": "WaveARGZ"
  },
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/WaveARGZ/shiori.git"
  },
  "main": "dist-electron/main/index.js",
  "scripts": {
    "clean": "node -e \"for(const d of ['dist','dist-electron'])require('fs').rmSync(d,{recursive:true,force:true})\"",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "tsc -p tsconfig.renderer.json",
    "check:renderer": "node scripts/check-renderer.mjs",
    "copy:assets": "node scripts/copy-assets.mjs",
    "build": "npm run clean && npm run build:main && npm run build:renderer && npm run check:renderer && npm run copy:assets",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "start": "npm run build && electron .",
    "smoke:fresh": "SHIORI_SMOKE=1 electron .",
    "smoke:restart": "SHIORI_SMOKE=restart electron .",
    "smoke": "npm run build && npm run smoke:fresh && npm run smoke:restart",
    "dist": "npm run build && electron-builder --mac",
    "dist:win": "npm run build && electron-builder --win",
    "dist:linux": "npm run build && electron-builder --linux"
  },
  "dependencies": {
    "electron-store": "8.2.0"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "electron": "43.1.1",
    "electron-builder": "26.15.3",
    "electron-builder-squirrel-windows": "26.15.3",
    "typescript": "5.9.3"
  }
}
